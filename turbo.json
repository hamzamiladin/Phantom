{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "render": { "dependsOn": ["^build"], "outputs": ["out/**"] }
  }
}
